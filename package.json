{
  "name": "w3-iron-action",
  "version": "0.1.0",
  "private": true,
  "description": "Iron fiat-to-crypto on/off-ramp, autoramps, and stablecoin operations for W3 workflows",
  "main": "src/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.js -o dist"
  },
  "type": "module",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@w3-io/action-core": "^0.3.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
